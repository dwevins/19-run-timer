<div class="main-view">
  <div class="container">
  <h1>New Run</h1>
    <form class="form" {{action 'addRun' on='submit'}}>
      {{input type='text' value=runTime placeholder='Run Time' class='form__input'}}
      {{input type='text' value=raceDate placeholder='Race Date' class='form__input'}}
      {{input type='text' value=raceNotes placeholder='Race Notes' class='form__input'}}

      <div class="form__buttons">
        {{#link-to 'index'}}
          <button type="button" class='form__buttons--btn cancel'>CANCEL</button>
        {{/link-to}}
        <button class="form__buttons--btn save">SAVE</button>
      </div>
    </form>
  </div>
</div>
{{outlet}}
